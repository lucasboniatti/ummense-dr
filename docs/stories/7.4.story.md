# Story 7.4: Unificação de API Client + Error Boundary + Polimento Auth

**Epic:** EPIC-7-UX-HARDENING
**Story ID:** 7.4
**Title:** Unificação de API Client + Error Boundary + Polimento Auth
**Status:** Done
**Story Points:** 8
**Priority:** P1 (High)
**Created:** 2026-03-09

---

## Executor Assignment

**Orchestrator:** @aios-master
**Implementation:** @ux-design-expert + @dev
**Quality Gate:** @qa

**Rationale:**
- **@ux-design-expert:** polir telas de login/signup com branding e identidade visual, criar Error Boundary visual.
- **@dev:** migrar services de `fetch` para `apiClient` (Axios) e integrar interceptors com `AuthContext`.
- **@qa:** valida que nenhum service usa `fetch` direto e que Error Boundary captura erros.

---

## Story

As a **developer and operations user**, I want to **ter um client de API unificado, proteção contra crashes visuais e telas de autenticação com identidade do produto**, so that **o código seja manutenível, erros não quebrem a aplicação e o primeiro contato do usuário com o produto transmita profissionalismo**.

---

## Acceptance Criteria

### Unificação de API Client

- [x] Todos os services usam exclusivamente `apiClient` (Axios) — nenhum `fetch` direto em services ou pages.
- [x] `apiClient` injeta `Authorization: Bearer {token}` automaticamente via interceptor.
- [x] Services migrados: `control.service.ts`, `webhook.service.ts`, `analytics.service.ts`, `history.service.ts`, `dlq.service.ts`.
- [x] Páginas migradas: `index.tsx` (painel), `admin/rate-limits.tsx`, `automations/settings.tsx`, `automations/audit-log.tsx`.
- [x] Interceptor de resposta 401 no `apiClient` dispara logout automático via `AuthContext`.

### Error Boundary

- [x] Existe `ErrorBoundary` global em `components/guards/` usando React class component.
- [x] Error Boundary exibe tela amigável: mensagem "Algo deu errado", botão "Recarregar página".
- [x] Error Boundary está integrado no `_app.tsx`, envolvendo todo o conteúdo protegido.
- [x] Erros de runtime não resultam em tela branca.

### Polimento Auth (Login/Signup)

- [x] Telas de login e signup possuem identidade visual do produto (logo/nome "Synkra").
- [x] Background com gradiente sutil ou cor de marca.
- [x] Card centralizado com sombra (`shadow-lg rounded-2xl`).
- [x] Link de navegação entre login ↔ signup ("Criar conta" / "Já tenho conta").
- [x] Layout responsivo (funciona em mobile e desktop).

---

## Tasks / Subtasks

### Unificação de API Client

- [ ] Migrar `src/services/control.service.ts` — classe `ControlService` usar `apiClient` (AC: 1, 3).
- [ ] Migrar `src/services/webhook.service.ts` — remover `request()` custom, usar `apiClient` (AC: 1, 3).
- [ ] Migrar `src/services/analytics.service.ts` — substituir `fetch` por `apiClient.post` (AC: 1, 3).
- [ ] Verificar e migrar `src/services/history.service.ts` (AC: 1, 3).
- [ ] Verificar e migrar `src/services/dlq.service.ts` (AC: 1, 3).
- [ ] Migrar `src/pages/index.tsx` — substituir `fetch` por services existentes (AC: 4).
- [ ] Migrar `src/pages/admin/rate-limits.tsx` — usar `apiClient` (AC: 4).
- [ ] Migrar `src/pages/automations/settings.tsx` — usar `apiClient` (AC: 4).
- [ ] Migrar `src/pages/automations/audit-log.tsx` — usar `apiClient` (AC: 4).
- [ ] Adicionar interceptor de 401 no `apiClient` que dispara logout (AC: 5).

### Error Boundary

- [ ] Criar `src/components/guards/ErrorBoundary.tsx` como class component (AC: 6, 7).
- [ ] Integrar no `src/pages/_app.tsx` envolvendo conteúdo protegido (AC: 8).

### Polimento Auth

- [ ] Redesenhar `src/pages/auth/login.tsx` com branding (AC: 10, 11, 12, 14).
- [ ] Redesenhar `src/pages/auth/signup.tsx` com branding (AC: 10, 11, 12, 14).
- [ ] Adicionar links de navegação no `LoginForm.tsx` e `SignupForm.tsx` (AC: 13).

---

## Technical Context for Dev Agent

### Migração de Services — Padrão

```typescript
// ANTES (control.service.ts):
async fetchQueueStatus(): Promise<QueueMetrics> {
    const res = await fetch('/api/status/queue');
    if (!res.ok) throw new Error('Failed to fetch queue status');
    return res.json();
}

// DEPOIS:
import { apiClient } from './api.client';

async fetchQueueStatus(): Promise<QueueMetrics> {
    const { data } = await apiClient.get<QueueMetrics>('/status/queue');
    return data;
}
```

### Interceptor de 401 (adicionar em `api.client.ts`)

```typescript
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      // Limpar token e redirecionar
      if (typeof window !== 'undefined') {
        window.localStorage.removeItem('synkra_dev_token');
        window.localStorage.removeItem('token');
        window.location.href = '/auth/login';
      }
    }
    return Promise.reject(error);
  }
);
```

### Error Boundary

```typescript
// src/components/guards/ErrorBoundary.tsx
// DEVE ser class component (React requirement)

class ErrorBoundary extends React.Component<
  { children: React.ReactNode },
  { hasError: boolean; error: Error | null }
> {
  static getDerivedStateFromError(error: Error) {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    console.error('[ErrorBoundary]', error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return (
        // Tela amigável centralizada
        // Botão "Recarregar" → window.location.reload()
      );
    }
    return this.props.children;
  }
}
```

### Polimento Auth — Layout Target

```tsx
// Layout de login/signup:
<div className="min-h-screen bg-gradient-to-br from-primary-50 to-primary-100 flex items-center justify-center p-4">
  <div className="bg-white shadow-lg rounded-2xl p-8 w-full max-w-md">
    <div className="text-center mb-8">
      <h1 className="text-3xl font-bold text-primary-700">Synkra</h1>
      <p className="text-neutral-500 mt-2">Plataforma de Operações</p>
    </div>
    {/* Form component */}
  </div>
</div>
```

### Integração Final no _app.tsx

```tsx
<AuthProvider>
  <ToastProvider>
    <ErrorBoundary>
      <AuthGuard>
        <AppShell>
          <Component {...pageProps} />
        </AppShell>
      </AuthGuard>
    </ErrorBoundary>
    <ToastContainer />
  </ToastProvider>
</AuthProvider>
```

---

## Testing

- [ ] Grep: nenhum `fetch(/api` ou `fetch(\`/api` em services após migração.
- [ ] E2E: token expirado → interceptor redireciona para login.
- [ ] E2E: provocar erro de runtime → Error Boundary exibe tela amigável ao invés de tela branca.
- [ ] Visual: login e signup exibem branding "Synkra" e gradiente.
- [ ] E2E: links "Criar conta" / "Já tenho conta" navegam corretamente.
- [ ] Build: `next build` passa sem erros.
- [ ] Lint: `npm run lint` passa.
- [ ] Typecheck: `npm run typecheck` passa.

---

## File List

**To create:**
- `packages/frontend/src/components/guards/ErrorBoundary.tsx`

**To modify:**
- `packages/frontend/src/services/api.client.ts`
- `packages/frontend/src/services/control.service.ts`
- `packages/frontend/src/services/webhook.service.ts`
- `packages/frontend/src/services/analytics.service.ts`
- `packages/frontend/src/services/history.service.ts`
- `packages/frontend/src/services/dlq.service.ts`
- `packages/frontend/src/pages/index.tsx`
- `packages/frontend/src/pages/admin/rate-limits.tsx`
- `packages/frontend/src/pages/automations/settings.tsx`
- `packages/frontend/src/pages/automations/audit-log.tsx`
- `packages/frontend/src/pages/_app.tsx`
- `packages/frontend/src/pages/auth/login.tsx`
- `packages/frontend/src/pages/auth/signup.tsx`
- `packages/frontend/src/components/LoginForm.tsx`
- `packages/frontend/src/components/SignupForm.tsx`

---

## Change Log

- **2026-03-09:** Story criada por `@sm` a partir dos WP-7, WP-9 e WP-10 do plano arquitetural do @architect.

# Story 10.11: Padronizar Formulários com Zod

**Epic:** EPIC-10-UX-DESIGN-SYSTEM-EXCELLENCE
**Story ID:** 10.11
**Title:** Padronizar Formulários com Zod
**Status:** In Review
**Story Points:** 8
**Priority:** P1 (High)
**Created:** 2026-03-09

---

## Executor Assignment

**Orchestrator:** @aios-master
**Implementation:** @dev
**Quality Gate:** @qa

---

## Story

As a **developer**, I want to **todos os formulários usarem react-hook-form + Zod consistentemente**, so that **a validação seja padronizada, type-safe e fácil de manter**.

---

## Acceptance Criteria

- [ ] `TaskForm.tsx` migrado para react-hook-form + Zod.
- [ ] `WebhookForm.tsx` migrado para react-hook-form + Zod.
- [ ] `LoginForm.tsx` já usa o padrão (verificar conformidade).
- [ ] Todos os formulários usam `FormField` component.
- [ ] Mensagens de erro consistentes e localizadas em PT-BR.
- [ ] Validação em tempo real (onBlur ou onChange conforme apropriado).
- [ ] Schemas Zod exportados em arquivos separados.

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

---

## Tasks / Subtasks

- [x] Revisar a camada atual de schemas em `packages/frontend/src/schemas/index.ts`.
- [x] Extrair `task` e `webhook` schemas para arquivos dedicados somente se isso reduzir acoplamento sem quebrar imports existentes.
- [x] Migrar `TaskForm.tsx` para react-hook-form + Zod.
- [x] Migrar `WebhookForm.tsx` para react-hook-form + Zod.
- [x] Verificar `LoginForm.tsx` segue padrão.
- [x] Documentar padrão de formulários.

---

## Dependencies

- Hard dependency: nenhuma.
- Sequencing note: se `10.4`, `10.5` ou `10.6` entregarem primitives novos usados nos formulários, esta story deve reutilizá-los em vez de manter controles nativos.

---

## Dev Notes

### Relevant Context

- `LoginForm.tsx` já usa `react-hook-form`, `zodResolver` e a camada de schemas compartilhados.
- `TaskForm.tsx` e `WebhookForm.tsx` ainda usam `useState` e validação local/manual.
- O repositório já possui `packages/frontend/src/schemas/index.ts`; a story deve evoluir essa base em vez de recriar a estrutura do zero.

### Schema Exemplo: Task

```tsx
// schemas/task.schema.ts
import { z } from 'zod';

export const taskFormSchema = z.object({
  title: z
    .string()
    .min(1, 'Título é obrigatório')
    .min(3, 'Título deve ter pelo menos 3 caracteres')
    .max(100, 'Título deve ter no máximo 100 caracteres'),
  description: z.string().max(500, 'Descrição muito longa').optional(),
  priority: z.enum(['low', 'medium', 'high'], {
    required_error: 'Selecione uma prioridade',
  }),
  dueDate: z.date().optional(),
  tags: z.array(z.string()).optional(),
});

export type TaskFormData = z.infer<typeof taskFormSchema>;
```

### Uso com React Hook Form

```tsx
import { useForm } from 'react-hook-form';
import { zodResolver } from '@hookform/resolvers/zod';
import { taskFormSchema, TaskFormData } from '@/schemas/task.schema';

const { register, handleSubmit, formState: { errors } } = useForm<TaskFormData>({
  resolver: zodResolver(taskFormSchema),
  defaultValues: { title: '', priority: 'medium' },
});
```

### Relevant Source Tree

- `packages/frontend/src/components/TaskForm.tsx`
- `packages/frontend/src/components/WebhookForm.tsx`
- `packages/frontend/src/components/LoginForm.tsx`
- `packages/frontend/src/components/composite/FormField.tsx`
- `packages/frontend/src/schemas/index.ts`

### Implementation Guidance

- Preservar o contrato externo de submit/cancel dos formulários existentes.
- Manter mensagens em PT-BR e consistentes com `LoginForm`.
- Se os schemas forem extraídos em arquivos dedicados, atualizar `index.ts` como barrel sem quebrar os imports atuais.

### Evidence Expectations

- Before/after de `TaskForm` e `WebhookForm`.
- Evidência de validação bloqueando submit inválido.

---

## Testing

- [ ] Unit: schemas validam corretamente.
- [ ] Unit: formulários exibem erros.
- [ ] E2E: submit de formulários funciona.
- [ ] E2E: validação impede submit inválido.

---

## File List

**Modified:**
- `README.md`
- `docs/guides/feedback-patterns.md`
- `packages/frontend/src/components/SignupForm.tsx`
- `packages/frontend/src/components/TaskForm.tsx`
- `packages/frontend/src/components/WebhookForm.tsx`
- `packages/frontend/src/schemas/index.ts`

**Verified:**
- `packages/frontend/src/components/LoginForm.tsx`

---

## Change Log

- **2026-03-09:** Story criada por `@sm` para Epic 10 Fase 3.
- **2026-03-10:** `@dev` padronizou `TaskForm` e `WebhookForm` com `react-hook-form` + Zod, alinhou o uso de schemas compartilhados e documentou o padrão no `README.md`.

---

## QA Results

### Review Date: _TBD_
### Reviewer: @qa (Quinn)
### Decision: _TBD_

Summary:
- _TBD_

Validation file:
- _TBD_

---

## PM Results

### Review Date: _TBD_
### Reviewer: @pm (Morgan)
### Decision: _TBD_

Summary:
- _TBD_

Validation file:
- _TBD_

# Story 10.15: Implementar Atalhos de Teclado

**Epic:** EPIC-10-UX-DESIGN-SYSTEM-EXCELLENCE
**Story ID:** 10.15
**Title:** Implementar Atalhos de Teclado
**Status:** In Review
**Story Points:** 5
**Priority:** P1 (High)
**Created:** 2026-03-09

---

## Executor Assignment

**Orchestrator:** @aios-master
**Implementation:** @dev
**Quality Gate:** @qa

---

## Story

As a **user**, I want to **usar atalhos de teclado para ações comuns**, so that **eu possa trabalhar de forma mais rápida e eficiente**.

---

## Acceptance Criteria

- [ ] Cmd/Ctrl+K abre busca global.
- [ ] Cmd/Ctrl+N cria nova tarefa.
- [ ] Cmd/Ctrl+/ toggle sidebar.
- [ ] Escape fecha modais e menus.
- [ ] ? abre help de atalhos.
- [ ] Atalhos documentados no app.
- [ ] Atalhos não conflitam com browser.

---

## 🤖 CodeRabbit Integration

> **CodeRabbit Integration**: Disabled
>
> CodeRabbit CLI is not enabled in `core-config.yaml`.
> Quality validation will use manual review process only.

---

## Tasks / Subtasks

- [x] Criar `packages/frontend/src/hooks/useKeyboardShortcuts.ts`.
- [x] Implementar listener global de teclado.
- [x] Mapear atalhos para ações.
- [x] Criar `packages/frontend/src/components/KeyboardShortcutsHelp.tsx`.
- [x] Adicionar help modal com `?`.
- [x] Integrar atalhos em AppShell.
- [x] Documentar atalhos no README.

---

## Dependencies

- Hard dependency: `10.14` deve definir a experiência de command palette antes do atalho `Cmd/Ctrl+K`.
- Sequencing note: os demais atalhos podem ser implementados em paralelo, desde que reutilizem o shell existente.

---

## Dev Notes

### Relevant Context

- `AppShell` e `AppTopbar` já concentram menu mobile, quick actions e busca/filtros.
- Esta story deve tratar conflitos de foco e não interferir em inputs, textareas ou campos editáveis.
- O modal de ajuda deve documentar apenas atalhos realmente entregues.

### Keyboard Shortcuts Map

| Atalho | Ação | Contexto |
|--------|------|----------|
| `Cmd/Ctrl + K` | Busca global | Global |
| `Cmd/Ctrl + N` | Nova tarefa | Global |
| `Cmd/Ctrl + /` | Toggle sidebar | Global |
| `Escape` | Fechar modal | Modal aberto |
| `?` | Help de atalhos | Global |

### Hook Implementation

```tsx
// hooks/useKeyboardShortcuts.ts
import { useEffect } from 'react';

interface Shortcut {
  key: string;
  ctrl?: boolean;
  meta?: boolean;
  shift?: boolean;
  action: () => void;
  description: string;
}

export function useKeyboardShortcuts(shortcuts: Shortcut[]) {
  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      for (const shortcut of shortcuts) {
        const match =
          e.key.toLowerCase() === shortcut.key.toLowerCase() &&
          !!shortcut.ctrl === (e.ctrlKey || e.metaKey) &&
          !!shortcut.shift === e.shiftKey;

        if (match) {
          e.preventDefault();
          shortcut.action();
          return;
        }
      }
    };

    window.addEventListener('keydown', handleKeyDown);
    return () => window.removeEventListener('keydown', handleKeyDown);
  }, [shortcuts]);
}
```

### Help Modal

```tsx
<KeyboardShortcutsHelp shortcuts={[
  { key: 'k', ctrl: true, description: 'Busca global' },
  { key: 'n', ctrl: true, description: 'Nova tarefa' },
  { key: '/', ctrl: true, description: 'Toggle sidebar' },
  { key: '?', description: 'Mostrar atalhos' },
]} />
```

### Relevant Source Tree

- `packages/frontend/src/components/layout/AppShell.tsx`
- `packages/frontend/src/components/layout/AppTopbar.tsx`
- `packages/frontend/src/components/TaskModal.tsx`
- `packages/frontend/src/components/ui/Dialog.tsx`
- `packages/frontend/src/hooks/useKeyboardShortcuts.ts`
- `packages/frontend/src/components/KeyboardShortcutsHelp.tsx`

### Implementation Guidance

- Ignorar atalhos globais quando o foco estiver em campo de edição ou conteúdo editável.
- Reutilizar o estado e o entry point de busca definidos na `10.14`.
- Garantir que `Escape` feche menus e diálogos sem conflitar com navegadores ou formulários.

### Evidence Expectations

- Tabela final de atalhos entregues no app.
- Evidência de `Cmd/Ctrl+K`, `Cmd/Ctrl+N`, `Cmd/Ctrl+/`, `Escape` e help modal.

---

## Testing

- [ ] Unit: atalhos disparam ações corretas.
- [ ] Unit: Escape fecha modais.
- [ ] E2E: atalhos funcionam em browser.
- [ ] Visual: help modal.

---

## File List

**Created:**
- `packages/frontend/src/components/KeyboardShortcutsHelp.tsx`
- `packages/frontend/src/hooks/useKeyboardShortcuts.ts`

**Modified:**
- `README.md`
- `packages/e2e/tests/product-parity.e2e.ts`
- `packages/frontend/src/components/layout/AppShell.tsx`

---

## Change Log

- **2026-03-09:** Story criada por `@sm` para Epic 10 Fase 4.
- **2026-03-10:** `@dev` implementou a camada global de atalhos, adicionou help modal no shell e documentou os comandos principais no `README.md`.

---

## QA Results

### Review Date: 2026-03-10
### Reviewer: @qa (Quinn)
### Decision: PASS

Summary:
- Segunda passada de QA concluiu que o concern anterior foi fechado. `useKeyboardShortcuts` deixou de registrar listeners duplicados, os gates tecnicos passaram e a smoke parity do Epic 10 manteve `Cmd/Ctrl+K`, help modal e shell operando como esperado no ambiente atual.

Validation file:
- [story-10.4-10.19-qa-gate-second-pass.md](/Users/lucasboniatti/Desktop/Projetos/ummense-dr/docs/qa/gates/story-10.4-10.19-qa-gate-second-pass.md)

---

## PM Results

### Review Date: _TBD_
### Reviewer: @pm (Morgan)
### Decision: _TBD_

Summary:
- _TBD_

Validation file:
- _TBD_

# Story 4.2: Backend Runtime Closure

**Epic:** EPIC-4-LOCAL-DELIVERY-HARDENING
**Story ID:** 4.2
**Title:** Backend Runtime Closure
**Status:** Done
**Story Points:** 5
**Priority:** P0 (Critical)
**Created:** 2026-03-03

---

## Executor Assignment

**Executor:** @dev
**Quality Gate:** @qa
**Quality Gate Tools:** ["coderabbit"]

**Rationale:**
- **Executor @dev:** backend dependency and runtime wiring is application implementation work.
- **Quality Gate @qa:** validates runtime integrity through deterministic startup and endpoint smoke checks.

---

## Story

As a **developer**, I want to **close backend runtime gaps and dependency alignment**, so that **the API starts reliably and supports local validation flows**.

---

## Acceptance Criteria

- [x] Backend runtime dependencies required by source imports are declared in `packages/backend/package.json`.
- [x] Backend starts via script (`dev` or `start`) without manual patching.
- [x] Health endpoint returns `200 OK`.
- [x] Startup fails fast with clear message when required env vars are missing.
- [x] Basic smoke route checks are documented.

---

## Tasks / Subtasks

- [x] Reconcile imports vs declared dependencies.
- [x] Add/adjust backend run scripts.
- [x] Add startup preflight for mandatory env vars.
- [x] Validate health endpoint in local boot checklist.

---

## Testing

- [x] Backend boots cleanly in local environment.
- [x] `/health` check passes.
- [x] Missing env simulation produces readable failure.

---

## File List

**Implemented:**
- `packages/backend/package.json` (updated runtime dependencies and typings)
- `packages/backend/src/server.ts` (startup preflight + fail-fast message + dynamic app load)
- `packages/backend/src/config/env.ts` (required env validation helpers)
- `packages/backend/src/lib/supabase.ts` (shared Supabase client)
- `packages/backend/src/utils/errors.ts` (`AppError` implementation)
- `packages/backend/src/middleware/auth.middleware.ts` (safe cookie token access)
- `packages/backend/src/services/webhook.service.ts` (runtime syntax fix in service object)
- `docs/guides/backend-local-smoke-checks.md` (boot + health + missing-env smoke runbook)
- `package-lock.json` (dependency lock refresh)

---

## Change Log

- **2026-03-03:** Story drafted by `@sm` for backend local reliability closure.
- **2026-03-03:** Story implementada por `@dev` com fechamento de runtime local, preflight de env e guia de smoke checks.
- **2026-03-03:** Story closed by `@po` após QA PASS e validação de completude dos critérios.

---

## Dev Agent Record

### Agent Model Used

GPT-5 Codex (Codex CLI)

### Debug Log References

- Sem arquivo de debug dedicado para esta story.

### Completion Notes List

- Declaradas dependências de runtime faltantes no backend e tipagens associadas.
- Corrigida quebra de parsing em `webhook.service.ts` que impedia boot (`generateSamplePayload`).
- Implementado preflight de variáveis obrigatórias (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `JWT_SECRET`) com falha explícita no startup.
- Criados módulos ausentes `src/lib/supabase.ts` e `src/utils/errors.ts` para alinhar imports existentes.
- Ajustado middleware de autenticação para leitura segura de cookie (`req.cookies?.token`).
- Documentado checklist de smoke local do backend em `docs/guides/backend-local-smoke-checks.md`.
- Evidências executadas:
  - `npm run start --workspace @ummense/backend` sem env obrigatório -> falha com mensagem clara.
  - `env -i ... npm run start --workspace @ummense/backend` sem `SUPABASE_SERVICE_ROLE_KEY` -> falha clara.
  - `env -i ... npm run start --workspace @ummense/backend` com env completo + `curl /health` -> HTTP 200.
  - `npm run lint`, `npm run typecheck`, `npm test` na raiz -> exit code 0.

### File List

- `packages/backend/package.json` (modified)
- `packages/backend/src/server.ts` (modified)
- `packages/backend/src/config/env.ts` (created)
- `packages/backend/src/lib/supabase.ts` (created)
- `packages/backend/src/utils/errors.ts` (created)
- `packages/backend/src/middleware/auth.middleware.ts` (modified)
- `packages/backend/src/services/webhook.service.ts` (modified)
- `docs/guides/backend-local-smoke-checks.md` (created)
- `docs/stories/4.2.story.md` (updated)
- `package-lock.json` (modified)

---

## QA Results

### Review Date: 2026-03-03
### Reviewer: @qa (Quinn)
### Decision: ✅ PASS

Summary:
- Evidence confirms Story 4.2 ACs are met for local runtime closure.
- Gate file: [story-4.2-qa-gate.md](/Users/lucasboniatti/Desktop/Projetos/ummense-dr/docs/qa/gates/story-4.2-qa-gate.md)

Notes:
- Non-blocking residual risk logged for `aws-sdk` v2 advisory (LOW severity) and should be handled in hardening backlog.
